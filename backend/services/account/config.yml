service:
  name: notification-service
  version: "1.0.0"

database:
  url: postgresql+asyncpg://postgres:13771379Am@localhost:5432/Account

redis:
  url: redis://localhost:6379

jwt:
  secret: test

api_ir:
  base_url: https://s.api.ir
  api_key: ${API_IR_API_KEY}
  timeout_seconds: 30

logging:
  level: INFO
